import { css } from "@emotion/core";

const water = "#00ffd1";
const pink = "#ff00b8";

const gradient = css`
  background: linear-gradient(to right, ${water} 0%, ${pink} 100%);
`;

export { pink, water, gradient };
